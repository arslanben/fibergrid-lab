# Copy to .env and adjust if you want. The lab works with the defaults below.
POSTGRES_DB=lds_gisdb
POSTGRES_PASSWORD=postgres_lab_pw
CBS_PASSWORD=cbs_lab_pw

# Host port served by the WAF (the only exposed surface of the lab).
LAB_PORT=8080
